<%= form_for(@custom_activity) do |f| %>
  <% if @custom_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@custom_activity.errors.count, "error") %> prohibited this custom_activity from being saved:</h2>

      <ul>
      <% @custom_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :completion_time %><br>
    <%= f.number_field :completion_time %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
